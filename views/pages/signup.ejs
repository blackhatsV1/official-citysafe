<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head'); %>
        
</head>

<body>
    <div>
        <!-- Auth Form Column -->
        <div>
            <div>
                <!-- Branding -->
                <div>
                    <a href="/visitor">
                        <div>
                            <i></i>
                        </div>
                        <span>CitySafe</span>
                    </a>
                </div>

                <!-- Intro -->
                <div>
                    <h2>Create Identity
                    </h2>
                    <p>Join the network to receive early
                        warning alerts and manage localized safety operations.</p>
                </div>

                <!-- Form -->
                <form method="POST" action="/register">

                    <div>
                        <div>
                            <label for="signup-first">First
                                Name</label>
                            <input id="signup-first" name="firstname" type="text" placeholder="John"
                                required>
                        </div>
                        <div>
                            <label for="signup-last">Last
                                Name</label>
                            <input id="signup-last" name="lastname" type="text" placeholder="Doe"
                                required>
                        </div>
                    </div>

                    <div>
                        <label for="signup-email">Email
                            Terminal</label>
                        <div>
                            <i></i>
                            <input id="signup-email" name="email" type="email"
                                placeholder="john.doe@example.com" required>
                        </div>
                    </div>

                    <div>
                        <label for="signup-contact">Comms
                            Frequency</label>
                        <div>
                            <i></i>
                            <input id="signup-contact" name="contact_number" type="text"
                                placeholder="+63 912 345 6789" required>
                        </div>
                    </div>

                    <div>
                        <label for="signup-password">Security
                            Passcode</label>
                        <div>
                            <i></i>
                            <input id="signup-password" name="password" type="password"
                                placeholder="••••••••" required>
                            <button type="button"
                                data-target="signup-password">
                                <i></i>
                            </button>
                        </div>
                    </div>

                    <div>
                        <label for="confirm-password">Confirm
                            Passcode</label>
                        <div>
                            <i></i>
                            <input id="confirm-password" name="confirm_password" type="password" placeholder="••••••••" required>
                            <button type="button"
                                data-target="confirm-password">
                                <i></i>
                            </button>
                        </div>
                    </div>

                    <div id="passError">
                        <i></i> Security passcodes do not align.
                    </div>

                    <button type="button" onclick="validateSignup()">
                        INITIALIZE CLEARANCE <i></i>
                    </button>
                </form>

                <!-- Footer text -->
                <div>
                    <p>
                        Already posess operational clearance? <br><a href="/login">Execute
                            Login Sequence</a>
                    </p>
                </div>

                <div>
                    <p>
                        Designed with <i></i> by CitySafe
                    </p>
                </div>
            </div>
        </div>

        <!-- Visual Hero Column -->
        <div>
            <!-- Huge abstract graphic background -->
            <div>
                <div></div>
                <div></div>

                <!-- Grid pattern overlay -->
                <div>
                </div>
            </div>

            <!-- Content -->
            <div>
                <div>
                    <div>
                        <div>
                            <i></i>
                        </div>
                    </div>
                    <h2>Join
                        CitySafe<br><span>Node Array</span></h2>
                    <p>
                        Protecting your sector starts with information. Get verified operational access to real-time
                        safety telemetry.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <!-- Hidden element to store email for OTP -->
    <div id="otpEmailDisplay"></div>

    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        // Password toggles
        document.querySelectorAll('.toggle-pass').forEach(btn => {
            btn.addEventListener('click', function () {
                const targetId = this.getAttribute('data-target');
                const passInput = document.getElementById(targetId);
                const icon = this.querySelector('i');

                if (passInput.type === "password") {
                    passInput.type = "text";
                    icon.classList.remove("fa-eye-slash");
                    icon.classList.add("fa-eye", "text-indigo-500", "dark:text-indigo-400");
                } else {
                    passInput.type = "password";
                    icon.classList.add("fa-eye-slash");
                    icon.classList.remove("fa-eye", "text-indigo-500", "dark:text-indigo-400");
                }
            });
        });

        // Validation & OTP flow using SweetAlert2
        function validateSignup() {
            const form = document.querySelector('form');
            if (!form.checkValidity()) {
                form.reportValidity();
                return false;
            }

            const p1 = document.getElementById("signup-password").value;
            const p2 = document.getElementById("confirm-password").value;
            const err = document.getElementById("passError");

            if (p1 !== p2) {
                err.classList.remove('hidden');
                return false;
            }
            err.classList.add('hidden');

            const formData = {
                firstname: document.getElementById('signup-first').value,
                lastname: document.getElementById('signup-last').value,
                email: document.getElementById('signup-email').value,
                contact_number: document.getElementById('signup-contact').value,
                password: p1
            };

            Swal.fire({
                title: 'Transmitting Clearance Request',
                text: 'Please wait while we establish your operational node...',
                allowOutsideClick: false,
                showConfirmButton: false,
                background: document.documentElement.classList.contains('dark') ? '#1f2937' : '#ffffff',
                color: document.documentElement.classList.contains('dark') ? '#f3f4f6' : '#111827',
                didOpen: () => { Swal.showLoading() }
            });

            fetch('/register', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(formData)
            })
                .then(res => res.json())
                .then(data => {
                    Swal.close();
                    if (data.success) {
                        document.getElementById('otpEmailDisplay').innerText = data.email;
                        showOtpModal(data.email);
                    } else {
                        Swal.fire({
                            icon: 'error',
                            title: 'Clearance Denied',
                            text: data.message || 'Registration sequence failed.',
                            background: document.documentElement.classList.contains('dark') ? '#1f2937' : '#ffffff',
                            color: document.documentElement.classList.contains('dark') ? '#f3f4f6' : '#111827'
                        });
                    }
                })
                .catch(err => {
                    Swal.close();
                    Swal.fire({
                        icon: 'error',
                        title: 'Comms Failure',
                        text: 'Unable to connect to the command center.',
                        background: document.documentElement.classList.contains('dark') ? '#1f2937' : '#ffffff',
                        color: document.documentElement.classList.contains('dark') ? '#f3f4f6' : '#111827'
                    });
                    console.error(err);
                });

            return false;
        }

        function showOtpModal(email) {
            Swal.fire({
                title: '<span>Verify Clearance</span>',
                html: `
                    <div>
                        Authentication packet dispatched to:<br>
                        <strong>${email}</strong>
                    </div>
                    <input type="text" id="otpInput" maxlength="4" placeholder="0000" onkeypress="return event.charCode >= 48 && event.charCode <= 57">
                    <div id="otpError">Invalid authorization code</div>
                `,
                showCancelButton: true,
                confirmButtonText: '<span>Authorize</span>',
                cancelButtonText: '<span>Abort</span>',
                confirmButtonColor: '#4f46e5',
                cancelButtonColor: '#6b7280',
                customClass: {
                    popup: 'glass-swal',
                    confirmButton: 'rounded-lg shadow-lg shadow-indigo-500/40',
                    cancelButton: 'rounded-lg'
                },
                preConfirm: () => {
                    const code = document.getElementById('otpInput').value;
                    if (code.length !== 4) {
                        Swal.showValidationMessage('Requires 4-digit code');
                        return false;
                    }
                    return code;
                }
            }).then((result) => {
                if (result.isConfirmed) {
                    verifyOtp(result.value);
                }
            });
        }

        function verifyOtp(code) {
            const email = document.getElementById('otpEmailDisplay').innerText;

            Swal.fire({
                title: 'Verifying Signal',
                allowOutsideClick: false,
                showConfirmButton: false,
                background: document.documentElement.classList.contains('dark') ? '#1f2937' : '#ffffff',
                color: document.documentElement.classList.contains('dark') ? '#f3f4f6' : '#111827',
                didOpen: () => { Swal.showLoading() }
            });

            fetch('/verify-code-json', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email, code })
            })
                .then(res => res.json())
                .then(data => {
                    if (data.success) {
                        Swal.fire({
                            icon: 'success',
                            title: 'Clearance Granted',
                            showConfirmButton: false,
                            timer: 1500,
                            background: document.documentElement.classList.contains('dark') ? '#1f2937' : '#ffffff',
                            color: document.documentElement.classList.contains('dark') ? '#f3f4f6' : '#111827'
                        }).then(() => {
                            window.location.href = data.redirect;
                        });
                    } else {
                        Swal.fire({
                            icon: 'error',
                            title: 'Clearance Denied',
                            text: data.message || "Invalid Authorization Code.",
                            confirmButtonText: 'Retry',
                            confirmButtonColor: '#4f46e5',
                            background: document.documentElement.classList.contains('dark') ? '#1f2937' : '#ffffff',
                            color: document.documentElement.classList.contains('dark') ? '#f3f4f6' : '#111827'
                        }).then(() => {
                            showOtpModal(email);
                        });
                    }
                })
                .catch(err => {
                    console.error(err);
                    Swal.close();
                });
        }
    </script>
</body>

</html>